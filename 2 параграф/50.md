№ 50
$$
\lim_{n \rightarrow \infty}{\frac{1 + a + a^2 + \cdots + a^n}{1 + b + b^2 + \cdots + b^n}} \qquad (|a|<1, \quad |b| < 1)
$$

\medskip

Фактически тут у нас суммы геометрических прогрессий:
$$
\lim_{n \rightarrow \infty}{\frac{1 + a + a^2 + \cdots + a^n}{1 + b + b^2 + \cdots + b^n}} = \lim_{n \rightarrow \infty}{
	\frac
		{\frac
			{1\cdot(1 - a^n)}
			{1 - a}
		}
		{\frac
			{1\cdot(1 - b^n)}
			{1 - b}
		}
} =
$$
Так как $|a| < 1$ и $|b| < 1$, то $a^n \rightarrow 0$ и $b^n \rightarrow 0$. Отсюда получается:
$$
$$
$$
= \lim_{n \rightarrow \infty}{
	\frac
		{\frac
			{1\cdot(1 - 0)}
			{1 - a}
		}
		{\frac
			{1\cdot(1 - 0)}
			{1 - b}
		}
} =
	\frac
		{\frac
			{1}
			{1 - a}
		}
		{\frac
			{1}
			{1 - b}
		} = \frac{1 - b}{1 - a}
$$
